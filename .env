# VITE_API_URL is intentionally left unset here so production builds (Vercel) fall
# back to the relative "/api" path, which vercel.json correctly routes to api/index.py.
# For local development, set VITE_API_URL in .env.local instead (gitignored),
# so it never gets committed or shipped in a deployed build.